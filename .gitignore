# macOS
.DS_Store
.AppleDouble
.LSOverride
Icon?

# Windows
Thumbs.db
Desktop.ini

# Editores
.vscode/
.idea/
*.swp

# Claude Code
# La configuración compartida del agente SÍ se sube:
#   CLAUDE.md, .claude/settings.json, .claude/skills/, .claude/commands/,
#   .claude/agents/, .claude/hooks/
# Lo que es local de cada alumno, no:
.claude/settings.local.json
.claude/state/
CLAUDE.local.md

# Temporales
*.tmp
*.bak
~$*

# Secretos — nunca subir credenciales
.env
.env.local
*.pem
*.key

# Artefactos de producción de video (pesados, se regeneran)
/build/
/render/
*.mp4
*.mov
*.wav
*.mp3

# Rendered video output — derived artefacts, rebuilt with `npm run render`
# in ../course-video-studio. The scene spec is the source of truth, not the mp4.
course/video/out/

# Video studio dependencies — reinstall with `npm install` in video-studio/
video-studio/node_modules/
